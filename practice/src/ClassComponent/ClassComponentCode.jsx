import  React from "react";

export default class ClassComponentCode extends React.Component{
    render(){
        return(
            <>
            <h1> How to write classcomponent code</h1>
            <h3> first import react .second export.than have to use render before the start the any code.3rd with use the return  </h3>
            </>
        )
       
    }
}